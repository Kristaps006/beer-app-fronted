// We add these compiler options to make it easier to import components

{

    "compilerOptions": {
        "baseUrl": ".",
        "paths":{
            "@/components/*":["components/*"],
            "@/styles/*":["styles/*"],
            "@/config/*":["config/*"]
        }
    }

}